## Packages
framer-motion | Smooth page transitions and micro-interactions
date-fns | Formatting dates for event cards
clsx | Utility for constructing className strings
tailwind-merge | Utility for merging Tailwind classes

## Notes
Mobile-first design approach:
- Bottom navigation for primary views
- Touch-friendly targets (min 44px)
- Swipe-friendly layouts
- Sticky call-to-action buttons on mobile
Auth is handled via existing hooks in `client/src/hooks/use-auth.ts`.
Images use Unsplash source URLs.
